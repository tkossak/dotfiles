#!/bin/bash

[[ -d "$HOME/.emacs.d" ]] && mv "$HOME/.emacs.d" "$HOME/.emacs.d.old"
[[ -d "$HOME/.spacemacs" ]] && mv "$HOME/.spacemacs" "$HOME/.spacemacs.old"
git clone --recursive https://github.com/syl20bnr/spacemacs ~/.emacs.d
